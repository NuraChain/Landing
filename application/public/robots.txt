# The dashboard is guarded by a key, not by this file - a disallow is a request, and the
# crawlers worth worrying about are the ones that ignore it. It is here so an honest crawler
# does not index a path that is no use to a reader.
User-agent: *
Disallow: /admin

Allow: /

# Generated from the blog store on request, not written to a file - see server/src/seo/sitemap.ts.
# The absolute url is required here: a relative path is the one thing the sitemap directive
# does not accept, and a crawler silently ignores the line rather than reporting it.
Sitemap: https://nurachain.net/sitemap.xml
